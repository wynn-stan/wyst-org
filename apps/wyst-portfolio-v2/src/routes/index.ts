// when creating new versions - replace "latest" with "vx", then create "latest" routes from scratch
const routes = {
  about: '/',
  work: {
    metric: '/works/metric',
    aag: '/works/aag',
    citizenappjobs: '/works/jobs',
    fsm: '/works/fsm',
  },
};

export default routes;
